import {sqliteTable,text,integer,index} from 'drizzle-orm/sqlite-core';
export const lists=sqliteTable('lists',{id:text('id').primaryKey(),owner:text('owner').notNull(),title:text('title').notNull(),period:text('period').notNull(),priority:integer('priority').notNull(),deadline:integer('deadline'),done:integer('done').notNull().default(0)},t=>[index('lists_owner').on(t.owner)]);
export const tasks=sqliteTable('tasks',{id:text('id').primaryKey(),owner:text('owner').notNull(),listId:text('list_id').notNull().references(()=>lists.id),title:text('title').notNull(),priority:integer('priority').notNull(),deadline:integer('deadline'),done:integer('done').notNull().default(0)},t=>[index('tasks_owner_list').on(t.owner,t.listId)]);
export const notes=sqliteTable('notes',{id:text('id').primaryKey(),owner:text('owner').notNull(),title:text('title').notNull(),content:text('content').notNull(),filename:text('filename'),fileKey:text('file_key'),size:integer('size')},t=>[index('notes_owner').on(t.owner)]);
export const mailState=sqliteTable('mail_state',{owner:text('owner').primaryKey(),revision:integer('revision').notNull().default(0),synced:integer('synced').notNull().default(0)});
